// DEPRECATED — Supabase has been removed from this project.
// Blog posts are now hardcoded as .md files in src/content/blog/ and loaded
// via src/lib/blogPosts.js. This stub remains only because the dev tooling
// could not delete the file directly; you can safely `git rm` it.
export const supabase = null;
